rules_version = '2';

/* サイトの情報には、ブラウザから直接触らせない。
   読み書きはすべて Cloud Function を通す（合言葉の確認をそこでやるため）。
   Function は管理者権限で動くので、このルールの影響を受けない。 */
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
