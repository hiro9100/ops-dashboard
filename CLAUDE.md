# ops-dashboard

`ops-dashboard_6.html` の1ファイル完結のダッシュボード（HTML + inline CSS/JS、ビルド不要）。
ページは `showPage(id)` で切り替え、`pageTitles` / `topbarActions` に登録する。
大きめのページは `<script>` 末尾のIIFEで `.content` に注入する方式（appdemo / flyer / payswitch が該当）。

## サービス名（重要）

月謝の集金・引き落としサービスは **「のびしろ＋」** — https://nobishiro-plus.com

- 乗り換え先（新サービス）としてUIに表示する名称はこれを使う。
- 「アスリートスコア」はクラブ向けの記録・管理アプリで、集金サービスとは別物。混同しないこと。
- 補足: このリモート実行環境のネットワークポリシーは nobishiro-plus.com への外向き接続を拒否する（プロキシが403を返す）ため、
  サービス仕様をサイトから直接確認することはできない。仕様が必要な場合はユーザーに確認する。

## 集金の乗り換えページ（page-payswitch）

現在利用中の引き落としサービスを選ぶと、乗り換えスケジュールが自動描画される。
- 日程は初回引き落とし日を起点に営業日ベースで逆算（土日のみ考慮、祝日は未考慮）。
- 各社の解約予告期間・停止依頼の締め日は一般的な「目安」。画面の詳細設定で上書きできる前提で、断定的な表記にしない。
